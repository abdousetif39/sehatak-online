    selectedDay &&
    format(selectedDay, "yyyy-MM-dd") === format(day, "yyyy-MM-dd")
      ? "text-blue-100"
      : "text-blue-600"
  }`}
>
  👥 {appointmentsCount[format(day, "yyyy-MM-dd")] || 0} {t("appointments")}
</div>

    </button>

  ))}

</div>

</div>
          
        </div>
      </div>
      {selectedDay && (
  <div className="mt-8 bg-white rounded-2xl border border-slate-200 p-6">
